# Nawaphat Portfolio

React + Vite portfolio with a TH / EN language switcher.

Run:
```bash
npm install
npm run dev
```

Build:
```bash
npm run build
```

Before publishing, replace the placeholder email and LinkedIn URL in `src/main.jsx`.
